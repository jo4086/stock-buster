import React from "react";
import { StyleSheet } from "react-native";
import { View, Text } from "./Themed";

export default function Input({ path }: { path: string }) {
  return (
    <View>
      <Text>input Text</Text>
    </View>
  );
}
